name: Track Upstream and Release

on:
  schedule:
    - cron: '17 */12 * * *'
  workflow_dispatch:
    inputs:
      tag:
        description: Official AdGuard Home tag to build, defaults to latest release
        required: false
        type: string

permissions:
  actions: write
  contents: write
  id-token: write
  attestations: write

concurrency:
  group: upstream-release
  cancel-in-progress: true

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.resolve.outputs.tag }}
      release_exists: ${{ steps.check.outputs.exists }}
    steps:
      - name: Resolve upstream release
        id: resolve
        uses: actions/github-script@v8
        env:
          TAG_INPUT: ${{ inputs.tag }}
        with:
          script: |
            const tagInput = (process.env.TAG_INPUT || '').trim();
            let tagName = tagInput;
            if (!tagName) {
              const { data } = await github.rest.repos.getLatestRelease({
                owner: 'AdguardTeam',
                repo: 'AdGuardHome'
              });
              tagName = data.tag_name;
            }
            core.setOutput('tag', tagName);
      - name: Check existing package release
        id: check
        uses: actions/github-script@v8
        env:
          RELEASE_TAG: ${{ steps.resolve.outputs.tag }}
        with:
          script: |
            try {
              await github.rest.repos.getReleaseByTag({
                owner: context.repo.owner,
                repo: context.repo.repo,
                tag: process.env.RELEASE_TAG
              });
              core.setOutput('exists', 'true');
            } catch (error) {
              if (error.status !== 404) throw error;
              core.setOutput('exists', 'false');
            }

  build:
    if: needs.prepare.outputs.release_exists == 'false' || github.event_name == 'workflow_dispatch'
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0
      - name: Build package on FreeBSD 15
        uses: vmactions/freebsd-vm@v1.4.8
        env:
          PORTVERSION: stable
        with:
          release: '15.0'
          usesh: true
          mem: 4096
          envs: PORTVERSION
          prepare: |
            export ASSUME_ALWAYS_YES=yes
            pkg bootstrap -f
            pkg update -f
            pkg install -f -y git ca_root_nss
          run: |
            chmod +x build.sh scripts/build-adguardhome-freebsd.sh
            ./scripts/build-adguardhome-freebsd.sh '${{ needs.prepare.outputs.tag }}'
            ./build.sh package
            cp build/pkg/*.pkg ./pfSense-pkg-adguardhome-stable.pkg
      - name: Attest package
        uses: actions/attest-build-provenance@v4.1.0
        with:
          subject-path: pfSense-pkg-adguardhome-stable.pkg
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: pfSense-pkg-adguardhome-stable
          path: pfSense-pkg-adguardhome-stable.pkg
      - name: Publish GitHub release
        uses: softprops/action-gh-release@v3.0.1
        with:
          tag_name: ${{ needs.prepare.outputs.tag }}
          name: ${{ needs.prepare.outputs.tag }}
          body: |
            pfSense package with the official stable AdGuard Home `${{ needs.prepare.outputs.tag }}` bootstrap binary, built for FreeBSD 15 amd64.
          overwrite_files: true
          files: |
            pfSense-pkg-adguardhome-stable.pkg

  cleanup:
    if: always() && needs.prepare.result == 'success'
    needs: [prepare, build]
    runs-on: ubuntu-latest
    steps:
      - name: Retain one previous run
        uses: actions/github-script@v8
        with:
          script: |
            const runs = await github.paginate(github.rest.actions.listWorkflowRuns, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'release.yml',
              status: 'completed',
              per_page: 100
            });
            const previousRuns = runs
              .filter((run) => run.id !== context.runId)
              .sort((a, b) => new Date(b.created_at) - new Date(a.created_at));

            for (const run of previousRuns.slice(1)) {
              await github.rest.actions.deleteWorkflowRun({
                owner: context.repo.owner,
                repo: context.repo.repo,
                run_id: run.id
              });
            }
